/**
 * Словарь маршрутизации: когда бот НЕ знает ответа,
 * он пересылает сообщение Дмитрию в ЛС.
 */
export const ROUTING = {
  // Ключевые слова, которые гарантированно требуют внимания человека
  'срочно': true,
  'проблема': true,
  'брак': true,
  'возврат': true,
  'жалоба': true,
  
  // ID получателя (Дмитрий) - подтягивается из .env
  // Но можно переопределить для тестов
  fallbackUserId: process.env.DMITRY_USER_ID || 1
};